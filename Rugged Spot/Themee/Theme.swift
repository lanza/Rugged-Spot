import UIKit

struct Theme {
    struct Fonts {
        static let main = UIFont(name: "Avenir Book", size: 18)
    }
    struct Colors {
        static let main = UIColor.init(named: "mainColor")?.cgColor
    }
}
